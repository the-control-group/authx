{
  "private": true,
  "workspaces": [
    "packages/authx",
    "packages/authx-interface",
    "packages/authx-manager",
    "packages/authx-proxy"
  ],
  "devDependencies": {
    "husky": "^1.1.2"
  },
  "nohoist": [
    "packages/authx-interface/html-webpack-plugin"
  ],
  "scripts": {
    "format": "for P in packages/*/; do cd $P && npm run format; cd ../..; done",
    "prepare": "for P in packages/*/; do cd $P && npm run prepare; cd ../..; done"
  }
}
