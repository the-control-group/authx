{
  "private": true,
  "workspaces": [
    "packages/authx",
    "packages/http-proxy-client",
    "packages/http-proxy-resource",
    "packages/interface",
    "packages/manager",
    "packages/scopes"
  ],
  "devDependencies": {
    "husky": "^2.1.0"
  },
  "nohoist": [
    "packages/authx-interface/html-webpack-plugin"
  ],
  "scripts": {
    "format": "for P in packages/*/; do echo \"\n\n---------- $P\" && cd $P && if ! (yarn format); then exit 1; fi; cd ../..; done",
    "lint": "for P in packages/*/; do echo \"\n\n---------- $P\" && cd $P && if ! (yarn lint); then exit 1; fi; cd ../..; done",
    "prepare": "for P in packages/*/; do echo \"\n\n---------- $P\" && cd $P && if ! (yarn prepare); then exit 1; fi; cd ../..; done"
  }
}
